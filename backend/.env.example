# Server Settings
PORT=5000
NODE_ENV=development

# Database Settings
# Replace with your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/ramdev_lucky_scratch?retryWrites=true&w=majority

# Security Settings
# Generate a secure random string for JWT
JWT_SECRET=supersecretjwtkeyforramdevmobilecampaign2026

# Default Admin Credentials (Auto-seeded if empty)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=RamdevMobile2026

# Allowed Frontend Origins (comma-separated)
CORS_ORIGIN=http://localhost:5173,https://yourfrontend.pages.dev

# Email SMTP Settings (Leave empty for fallback Console/Development logger)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@newramdevmobile.com
